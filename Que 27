pri = float(input("enter the prinicipal amount: "))
rate = float(input("enter the rate of interest: "))
time = float(input("enter the time in years: "))

SI = (pri * rate * time) / 100
print("The simple interest is:", SI)